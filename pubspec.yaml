name: signwriting
description: Dart utilities for SignWriting formats, tokenizer and visualizer.
version: 1.2.1
repository: https://github.com/bipinkrish/signwriting-dart
topics:
  - sign
  - signwriting

environment:
  sdk: ^3.0.0

dependencies:
  tuple: ^2.0.2

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
